#include <bits/stdc++.h>
#include "scan.h"
using namespace std;

int main()
{
    freopen("SAMPLE_r.TNY", "r", stdin);
    TokenType token;
    while(!EOFFlag && (token = getToken()) != ERROR) {}
    return 0;
}
